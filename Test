# Run Pester and store the results in a variable
$result = Invoke-Pester -Script .\Testservice.ps1 -PassThru

# Generate an HTML report from the results and save it without BOM
$result | Format-Pester -Format Html -ReportTitle "Report for Service" | Set-Content -Path "report.html" -Encoding UTF8
